export type BotrixUserType = {
    level: number,
    watchtime: number,
    xp: number,
    points: number,
    name: string,
    followage: {
        date: string,
        timezone_type: 1,
        timezone: string
    }
}